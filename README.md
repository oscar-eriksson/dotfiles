# dotfiles

## Install

```bash
cd $HOME

# clone this repo
git clone

cd dotfiles
chmod +x ./install/bootstrap.sh && ./install/bootstrap.sh
```

## Links
```bash
# links.prop
source=dest
$DOTFILES/zsh/rc.zsh=$HOME/.zshrc
```
